GROQ_API_KEY=your_api_key
GOOGLE_API_KEY=your_api_key # Using the free-tier model in CrewAI as a fallback when the primary Llama 4 Scout model fails.
OPENAI_API_KEY=your_api_key # If you're using RAGA for RAG evaluation, it defaults to OpenAI as the evaluation model.
